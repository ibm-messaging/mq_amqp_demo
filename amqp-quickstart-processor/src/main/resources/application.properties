# Set the AMQP address for the `requests` channel, as it's not the channel name
mp.messaging.incoming.requests.address=quote_requests

amqp-host=localhost
amqp-port=5672

amqp-username=app
amqp-password=passw0rd

# Enable to use queues in place of topics 
# Make sure that the queues quotes and quote_requests exist
# mp.messaging.outgoing.quotes.capabilities=queue
# mp.messaging.incoming.quote_requests.capabilities=queue
# mp.messaging.incoming.quote_requests.connector=smallrye-amqp
